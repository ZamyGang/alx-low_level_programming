#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * malloc_checked - A function that allocates memory using malloc.
 * @b: input
 * Return: 0 (success).
 */
void *malloc_checked(unsigned int b)
{
	int *a = malloc(a);
	
	if (a == NULL)
	{
		exit(98);
	}
	return (a);
}
