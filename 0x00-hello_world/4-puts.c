#include <stdio.h>
/*
 * main - Entry point
 * return: 0
 * print the variable on the console
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}
