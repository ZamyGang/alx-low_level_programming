#include <stdio.h>
/*
 */
int main(void)
{

	
	
	char lower = 'a';
	char upper = 'A';

	for (lower = 'a'; lower <= 'z' ; alpha++)
	{
		putchar(lower);
	}
	for (upper = 'A'; lower <= 'Z' ; lower++)
	{
		putchar(upper);
	}
	putchar('\n');
	return (0);
}
