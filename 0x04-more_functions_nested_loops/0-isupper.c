#include "main.h"
#include <stdio.h>
/**
 * isupper - A function that checks for uppercase character.
 *
 * Return: 0 (success)
 */

int main(void)
{
	char c;

	if (c >= 'A' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
