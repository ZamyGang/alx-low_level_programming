#include "main.h"
#include <stdio.h>
/**
 * isupper - A function that checks for uppercase character.
 *
 * Return: 0 (success)
 */

int main(void)
{
	int c;

	if ( c >= 61 && c <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
